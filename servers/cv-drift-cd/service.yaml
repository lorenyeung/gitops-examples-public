apiVersion: v1
kind: Service
metadata:
  name: cv-drift-cd
  namespace: tmpgitops
  labels:
    app: cv-drift-cd
spec:
  selector:
    app: cv-drift-cd
  ports:
  - name: http
    port: 8080
    targetPort: 8080
