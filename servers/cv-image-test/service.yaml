apiVersion: v1
kind: Service
metadata:
  name: cv-image-test
  namespace: tmpgitops
  labels:
    app: cv-image-test
spec:
  selector:
    app: cv-image-test
  ports:
  - name: http
    port: 8080
    targetPort: 8080
